<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:rabbit="http://www.springframework.org/schema/rabbit"
       xsi:schemaLocation="http://www.springframework.org/schema/rabbit http://www.springframework.org/schema/rabbit/spring-rabbit.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">

    <!-- 消费者 监听配置-->
    <!-- The acknowledge mode: "auto", "manual", or "none".-->
    <!-- auto -->
    <!--<rabbit:listener-container connection-factory="mqConnectionFactory"
                               acknowledge="auto" concurrency="1"
                               message-converter="jsonMessageConverter" prefetch="1">
        &lt;!&ndash; 一个监听器可以同时监听多个队列中的消息&ndash;&gt;
        <rabbit:listener ref="aotuConsumer" queues="aa,bb" method="onMessage"/>
        <rabbit:listener ref="aotuConsumer" queues="cc" method="onMessage"/>
    </rabbit:listener-container>-->
    <!-- manual -->
    <rabbit:listener-container connection-factory="mqConnectionFactory"
                               acknowledge="manual" concurrency="1"
                               message-converter="jsonMessageConverter" prefetch="1">
        <rabbit:listener ref="investerConsumer" queues="ff" method="onMessage"/>
    </rabbit:listener-container>
</beans>